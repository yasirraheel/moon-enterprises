<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/dialog_rounded_background"
    android:padding="0dp">

    <!-- Header with Gradient Background -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/maintenance_header_background"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center">

        <!-- Maintenance Icon with Animation -->
        <androidx.cardview.widget.CardView
            android:layout_width="60dp"
            android:layout_height="60dp"
            app:cardCornerRadius="30dp"
            app:cardElevation="8dp"
            app:cardBackgroundColor="@color/white"
            android:layout_marginBottom="12dp">

            <ImageView
                android:id="@+id/iv_maintenance_icon"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_maintenance_animated"
                android:contentDescription="Maintenance"
                android:scaleType="fitCenter" />

        </androidx.cardview.widget.CardView>

        <!-- Title -->
        <TextView
            android:id="@+id/tv_maintenance_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Service Temporarily Unavailable"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:gravity="center"
            android:layout_marginBottom="6dp" />

        <!-- Subtitle -->
        <TextView
            android:id="@+id/tv_maintenance_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="We're working to improve your experience"
            android:textSize="12sp"
            android:textColor="@color/white"
            android:alpha="0.9"
            android:gravity="center" />

    </LinearLayout>

    <!-- Content Area -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Main Message -->
        <TextView
            android:id="@+id/tv_maintenance_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Our team is currently performing scheduled maintenance to enhance the app's performance and add new features. This process usually takes a few minutes."
            android:textSize="14sp"
            android:textColor="@color/text_primary"
            android:lineSpacingMultiplier="1.2"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <!-- Progress Indicator -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="20dp">

            <ProgressBar
                android:id="@+id/progress_maintenance"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:indeterminateTint="@color/primary_color"
                android:layout_marginEnd="10dp" />

            <TextView
                android:id="@+id/tv_progress_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Please wait..."
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                android:textStyle="italic" />

        </LinearLayout>

        <!-- Estimated Time (Optional) -->
        <LinearLayout
            android:id="@+id/layout_estimated_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="@drawable/maintenance_info_background"
            android:padding="10dp"
            android:layout_marginBottom="16dp"
            android:visibility="gone">

            <ImageView
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:src="@drawable/ic_time"
                android:contentDescription="Time"
                android:tint="@color/primary_color"
                android:layout_marginEnd="6dp" />

            <TextView
                android:id="@+id/tv_estimated_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Estimated completion: 5-10 minutes"
                android:textSize="12sp"
                android:textColor="@color/text_secondary" />

        </LinearLayout>

    </LinearLayout>

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="@drawable/dialog_footer_background"
        android:gravity="center">

        <!-- Retry Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_retry"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:text="Retry"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:backgroundTint="@color/primary_color"
            android:textAllCaps="false"
            android:layout_marginEnd="8dp"
            app:cornerRadius="22dp"
            app:elevation="4dp"
            app:icon="@drawable/ic_refresh"
            app:iconGravity="textStart"
            app:iconPadding="6dp"
            style="@style/Widget.MaterialComponents.Button" />

        <!-- Exit Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_exit"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:text="Exit"
            android:textSize="14sp"
            android:textColor="@color/danger_color"
            android:backgroundTint="@android:color/transparent"
            android:textAllCaps="false"
            android:layout_marginStart="8dp"
            app:cornerRadius="22dp"
            app:icon="@drawable/ic_exit"
            app:iconGravity="textStart"
            app:iconPadding="6dp"
            app:strokeColor="@color/danger_color"
            app:strokeWidth="2dp"
            style="@style/Widget.Material3.Button.OutlinedButton" />

    </LinearLayout>

</LinearLayout>
